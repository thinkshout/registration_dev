<?php

class RegistrationDevStandardTestCase extends RegistrationStandardTestCase {
  protected $profile = 'registration_dev';

  public static function getInfo() {
    $info = parent::getInfo();
    $info['name'] = 'Registration dev profile';
    $info['description'] = 'Test Registration using dev profile.';
    return $info;
  }

}

class RegistrationDevWaitlistTestCase extends RegistrationWaitlistTestCase {
  protected $profile = 'registration_dev';

  public static function getInfo() {
    $info = parent::getInfo();
    $info['name'] = 'Registration dev profile waitlist';
    $info['description'] = 'Test Registration Wait List using dev profile.';
    return $info;
  }

}
